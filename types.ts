export interface Service {
  icon: React.ElementType;
  title: string;
  description: string;
  image: string;
}

export interface Testimonial {
  image: string;
  name: string;
  rating: number;
  quote: string;
}